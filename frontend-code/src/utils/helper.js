export function initialValues() {
    return { name: "", is_active: true };
}

// export function defineFormType(selectedVal, text) {
//     console.log(text)
//     return selectedVal ? `Edit ${text}` : `Create ${text}`;
// };
